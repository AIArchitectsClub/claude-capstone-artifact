// Generated by the e2e-perfromance-test-suite-k6 skill.
// Shared HTTP helpers — base URL and auth header construction, adjusted per
// target app's stack-detection profile (see references/stack-detection.md).
// Profile A (bearer-token auth, this reference app): as written below.
// Profile B (cookie-session auth): swap authHeaders() for a cookie jar
// per VU instead — see stack-detection.md's Profile B note.

const BASE_URL = __ENV.BASE_URL || 'http://127.0.0.1:{{BACKEND_TEST_PORT}}';

export function apiUrl(path) {
  return `${BASE_URL}${path}`;
}

export function jsonHeaders() {
  return { headers: { 'Content-Type': 'application/json' } };
}

export function formHeaders() {
  return { headers: { 'Content-Type': 'application/x-www-form-urlencoded' } };
}

export function authHeaders(token) {
  return { headers: { Authorization: `Bearer ${token}`, 'Content-Type': 'application/json' } };
}
