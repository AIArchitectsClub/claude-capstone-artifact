// Generated by the e2e-perfromance-test-suite-k6 skill.
// Always run this before the full NFR profile (see
// references/k6-conventions.md's "Warm-up before the real run"). 1 VU, a
// couple of iterations of each flow, fails fast on a wrong port, broken
// auth, or a scenario-code bug — in seconds, not minutes.
import { bookingLifecycle } from './booking-lifecycle.js';
import { browseAndPrice } from './browse-and-price.js';

export const options = {
  scenarios: {
    smoke_sessions: { executor: 'shared-iterations', vus: 1, iterations: 2, exec: 'bookingLifecycle' },
    smoke_throughput: { executor: 'shared-iterations', vus: 1, iterations: 2, exec: 'browseAndPrice' },
  },
  thresholds: {
    http_req_failed: ['rate<0.5'], // loose — this is a wiring check, not an NFR gate
  },
};

export { bookingLifecycle, browseAndPrice };
